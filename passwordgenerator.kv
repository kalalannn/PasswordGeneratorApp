<PasswordManagerWidget>
	Button:
		text: 'I am here'

<PasswordGeneratorWidget>
	orientation: 'vertical'
	ConfigurationWidget:
		id: config
		size_hint: (1, .35)
	OutputWidget:
		id: output
		size_hint: (1, .65)

<SettingsWidget>
	Button:
		text: 'Settings'


<MenuWidget@BoxLayout>
	id: box
	orientation: 'horizontal'
	spacing: 1
	rows: 3
	Button:
		text: 'Generator'
		on_press: app.root.next_state('PasswordGeneratorWidget')
	Button:
		text: 'Password Manager'
		on_press: app.root.next_state('PasswordManagerWidget')
	Button:
		text: 'Settings'
		on_press: app.root.next_state('SettingsWidget')

<CheckBoxWidget@BoxLayout>
	orientation: 'horizontal'
	size_hint: (.35, 1)
	CheckBox:
		size_hint: 0.15, 1
	Label:
		text: 'CheckBox'
		valign: 'center'

<ConfigurationWidget@BoxLayout>
	orientation: 'horizontal'
	BoxLayout:
		orientation: 'vertical'
		CheckBoxWidget:
	Button:
		text: 'Some'
<OutputWidget@BoxLayout>
	Button:
		text: 'kok4os'

RootWidget:
	id: box
	orientation: 'vertical'
	padding: [2, 1, 2, 0]
	spacing: 3
	rows: 3
	
	MenuWidget:
		id: menu
		size_hint: (1, .1)
	PasswordGeneratorWidget:
		id: widg
		size_hint: (1, .9)
